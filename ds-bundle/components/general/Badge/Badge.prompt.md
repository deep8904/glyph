Badge from glyph. Use via `window.GlyphDS.Badge` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Preview

```jsx
() => <Badge variant={"default"} size={"md"}>{"Indie Dev"}</Badge>
```
